name: api_client
description: Robust Dio client with JWT refresh coordination, DRF error parsing, and AppError mapping.
version: 0.1.0
homepage: https://example.com
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"
dependencies:
  dio: ^5.9.0
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
flutter:
  uses-material-design: false
